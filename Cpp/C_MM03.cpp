#include <iostream>

using namespace std;

int main()
{
    int num1, num2;
    while(cin >> num1 >> num2)          //check the input exists or not
    {
        cout << num1 + num2 << endl;       //sum two inputs
    }
    return 0;
}
